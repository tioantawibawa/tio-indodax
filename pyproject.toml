[project]
name = "indodax-agent"
version = "0.1.0"
description = "Autonomous crypto trading agent for Indodax with code-enforced hard risk limits"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"
